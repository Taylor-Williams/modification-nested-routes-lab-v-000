<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <% if @song.artist %>
    <%= f.hidden_field :artist_id %>
  <% else %>
    <%= f.label :artist_name %>
    <%= f.text_field :artist_name %>
  <% end %>
  <br />

  <%= f.submit %>
<% end %>
